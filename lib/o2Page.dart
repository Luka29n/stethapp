import 'package:flutter/material.dart';

class O2page extends StatelessWidget {
  const O2page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("O₂"),
      ),
      
    );
  }
}
