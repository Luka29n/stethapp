import 'package:flutter/material.dart';

class DataLog extends StatelessWidget {
  const DataLog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Data Log"),
      ),
      
    );
  }
}
